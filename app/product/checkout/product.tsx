import React from "react"
type props = {
	[x: string]: any
	qty: string
	price: string | number
	delete?: any
	change?: any
}
const product = (props: props) => {
	return <div>product</div>
}

export default product
